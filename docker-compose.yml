services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: irbis
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: irbis
      DB_USER: postgres
      DB_PASSWORD: postgres
      PORT: 5000
      VAPID_PUBLIC_KEY: BJvS2vZAj0ssCr8TXl3yZx4nu5zvDq_DDUzMe4Ogi4DQvpp9fwI0YU5RKDSwlBl0Jza2RZBFD3JW9yRayEOEZ58
      VAPID_PRIVATE_KEY: A7cROvNM_yP5TP8GCoXKfihOTnqi8zrZX1tpKm86_EI
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./server:/app
      - /app/node_modules
      - irbis_uploads:/app/uploads
    healthcheck:
      test: ["CMD-SHELL", "node -e 'fetch(\"http://localhost:5000/api/sites\").then(()=>process.exit(0)).catch(()=>process.exit(1))'"]
      interval: 5s
      timeout: 5s
      retries: 10

  frontend:
    build: ./client
    ports:
      - "5173:80"
    depends_on:
      backend:
        condition: service_healthy

volumes:
  pgdata:
  irbis_uploads:
